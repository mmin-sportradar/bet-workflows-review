const buttons=document.querySelectorAll('.team-button'),cards=document.querySelectorAll('.stage-card'),dialogs=document.querySelectorAll('dialog');function selectTeam(team){buttons.forEach(b=>{const s=b.dataset.team===team;b.classList.toggle('selected',s);b.setAttribute('aria-pressed',s)});cards.forEach(c=>{const on=team==='all'||c.dataset.owner===team;c.disabled=!on;c.classList.toggle('is-dependency',!on);c.setAttribute('aria-disabled',!on)})}buttons.forEach(b=>{b.setAttribute('aria-pressed',b.classList.contains('selected'));b.addEventListener('click',()=>selectTeam(b.dataset.team))});cards.forEach(c=>c.addEventListener('click',()=>{const d=document.getElementById(c.dataset.dialog);if(!c.disabled&&!d.open)d.showModal()}));dialogs.forEach(d=>{d.querySelector('.dialog-close').addEventListener('click',()=>d.close());d.addEventListener('click',e=>{if(e.target===d)d.close()})});